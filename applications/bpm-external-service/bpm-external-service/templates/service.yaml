apiVersion: v1
kind: Service
metadata:
  name: {{ include "bpm-external-service.fullname" . }}
  labels:
    {{- include "bpm-external-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bpm-external-service.selectorLabels" . | nindent 4 }}
